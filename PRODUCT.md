# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**1순위 — 프랜차이즈에서 먹을 메뉴를 고르는 실사용자.** 이동 중 모바일에서, 새로 나온 메뉴나 눈앞의 메뉴가 내 목표(다이어트·단백질 등)에 맞는지 빠르게 판단하려는 사람. 한 손 조작·짧은 체류가 기본 조건이다.

**2순위 — 채용 담당자와 멘토.** 데스크톱에서 이 서비스를 열어 데이터 파이프라인의 깊이와 판단 근거를 확인한다. 실사용자 경험을 희생해 근거를 전시하지는 않되, 데스크톱 화면도 "제대로 만든 것"으로 성립해야 한다.

두 청중의 우선순위는 확정: **실사용자가 먼저이고, 그 완성도 자체가 포트폴리오 증거가 된다.**

## Product Purpose

브랜드마다 흩어지고 형식도 제각각인 프랜차이즈 메뉴 영양정보를 **동일한 기준으로 정규화해 비교 가능하게 만들고**, 그 위에서 **새로 나온 메뉴를 분석하고 개인 기준에 맞는 메뉴를 추천한다.** 매장 위치는 "그래서 어디서 먹나"에 답하는 보조 연결이다.

성공 = 사용자가 목표에 맞는 메뉴를 근거와 함께 골라내는 것. 부수적으로, 그 판단을 떠받치는 수집→검증→적재→가공 파이프라인이 서비스 화면에서 드러나는 것.

기획 이력상 최초 가설("다이어트 중 갈 식당이 없다")은 검증되지 않은 개인적 추정이라는 지적으로 폐기됐고, 현재 문제 정의는 46개 브랜드를 실제 조사하며 데이터로 확인한 사실에 근거한다. 이 재정의를 되돌리지 않는다.

2026-09 중심축 이동: 진입 화면을 지도에서 신메뉴 분석·개인화 추천으로 옮겼다. 로컬 식당은 영양정보를 공개하지 않고 다이어트에 유리한 브랜드는 한정돼 있어, "주변 매장 찾기"를 앞세우면 같은 매장만 반복 추천되기 때문이다. 바뀐 것은 전달 면이지 문제 정의가 아니다 — "흩어진 영양정보를 한 기준으로 비교 가능하게"는 그대로다.

## Positioning

**브랜드 공식 영양정보를 100kcal 기준으로 정규화하고, 그 위에 절대·상대 이중 등급을 얹은 뒤, 매장 위치와 붙여 놓은 곳.** 이웃 서비스가 그대로 베낄 수 없는 지점은 셋이다.

- **100g이 아니라 100kcal 기준 정규화** — 샐러디가 중량(g)을 공개하지 않아 100g 기준으로는 공정 비교가 깨진다. 모든 브랜드가 가진 칼로리를 축으로 삼았다.
- **점수는 절대(WHO·식약처·논문 인용 고정값), 등급 밴드만 상대화** — 절대만 쓰면 패스트푸드 특성상 거의 전부 D, 상대만 쓰면 근거가 약하다. 둘 다 저장한다.
- **적재 전 품질 게이트** — 파서가 조용히 깨져도 UPSERT가 정상 데이터를 덮어쓰지 않는다. "한 브랜드 항목의 30% 초과가 동시에 같은 필드에서 움직이면 파서 버그"라는 판정 휴리스틱을 쓴다.

## Operating Context

- **모바일 우선, 데스크톱도 제대로.** 실사용은 이동 중 모바일, 심사는 데스크톱. 양쪽 모두 완성도가 필요하다.
- 진입 지점은 신메뉴다(해시가 없으면 `#new`). 입력·로그인·위치 없이 바로 볼 것이 있고 주 2회 내용이 바뀐다. 지도는 `#map`으로 열리는 보조 뷰이며, 처음 열 때만 카카오 SDK와 매장 데이터를 불러온다.
- 현재 화면 구성: 신메뉴 / 맞춤 추천 / 지도 / 대시보드 / 매장 목록 5개 뷰, URL 해시가 곧 현재 뷰(공유 가능).
- 데이터는 실시간이 아니다. 월 1회(메뉴)·주 1회(매장) 배치로 갱신되며, 화면에는 마지막으로 품질 게이트를 통과한 크롤 날짜가 기준일로 표시된다.
- 배포는 CloudFront(S3) → Lambda Function URL(FastAPI) → Neon PostgreSQL. 리전 제약(ap-southeast-2)과 DB 왕복 ~90ms 때문에 화면당 API 호출 수가 곧 체감 속도다. Function URL 응답 상한 6MB — 전국 매장을 한 번에 부르는 화면은 성립하지 않고, 프론트는 항상 반경을 넘긴다.

## Capabilities and Constraints

**있는 것**

- 브랜드 목록 / 브랜드별 메뉴 + 영양정보 + 등급 / 브랜드 평균·등급 요약
- 좌표·반경 기반 주변 매장 조회, 등급 필터(절대·상대 선택), 거리순 정렬
- 메뉴 탐색기, 목표별 메뉴 추천(신체정보 기반 BMR 포함), 신메뉴 피드, 품질·통계 대시보드
- 등급 이중 체계: `score`(절대) + `absolute_grade` + `relative_grade` + `percentile`을 함께 저장·표시
- 영양소는 고정 컬럼이 아닌 key-value 행(`nutrition_fact`)으로 저장 — 브랜드마다 공개 항목이 다르기 때문(대부분 5종, 샐러디 7종, 포케올데이 9종)
- 이력·품질 테이블(append-only): 크롤 실행, 메뉴/영양 스냅샷, 품질 체크, 변경 로그

**용어** — 등급 A~D, 절대 등급 / 상대 등급, 다이어트 점수, 기준일, 브랜드(=프랜차이즈), 매장(=지점).

**제약**

- 100kcal 미만 메뉴는 등급 산정에서 제외(블랙커피가 밀도 왜곡으로 A가 되는 문제).
- 세트 메뉴는 전면 제외 — 칼로리 "범위"만 공개돼 고정 영양값이 없다.
- 가격 데이터는 없다. 46개 브랜드 중 영양정보와 가격을 함께 공개하는 곳은 2곳뿐이라 슈링크플레이션 기획을 접었다.
- 맘스터치는 영양정보를 이미지로만 공개해 수기 관리(버거류만 반영), 버거킹은 WAF 차단으로 수동.
- `menu_item`은 UPSERT만 하고 DELETE하지 않아 단종 메뉴가 남아 있을 수 있다.
- 매장 좌표는 카카오맵 기준이라 폐업 미삭제·중복 등록이 섞일 수 있다.
- 절대 기준 A등급은 사실상 존재하지 않는다("프랜차이즈에 흠잡을 데 없는 다이어트 메뉴는 없다"는 결과 자체이기도 하다). 나트륨 기준은 다른 지표보다 가혹하게 작동한다.
- 사용자 행동 로그는 GA4 이벤트 수준만 있고, KPI(참여율·필터 사용률·전환율)를 산출할 집계는 아직 없다.

**미확정** — 접근성 기준(WCAG 등급 등)은 정해진 바 없음.

**로그인** — 선택 사항이다. 로그인 없이도 모든 기능이 동작하고, 개인화 설정은 로컬 저장으로
유지된다. 카카오 로그인을 하면 같은 설정이 서버에 저장돼 기기 간에 이어진다.

수집 항목은 카카오 닉네임과 사용자가 직접 입력한 추천 설정(목표·한 끼 상한·성별·키·몸무게·
나이·활동량), 그리고 추천 카드 클릭·저장·숨기기 이력뿐이다. 이메일·생일 등 추천에 쓰지 않는
항목은 카카오 동의 단계에서 요구하지 않는다. `DELETE /api/me` 한 번으로 계정과 함께 전부 삭제된다.

**개인 추천** — 로그인 사용자에게 "오늘 당신에겐" 3개를 보여준다. 무료 사용자는 서버 안의 규칙
(한 끼 적정 열량·영양 균형·저장/클릭 기록)으로 고르며 외부로 아무것도 보내지 않는다.

기록이 쌓이면 무료 추천도 **그 사람의 저장·빼기·무시 기록으로 취향을 학습**해 순서를 조정한다
(서버 안의 작은 통계 모델, 외부 전송 없음, 호출 비용 없음). 학습은 목표 안에서만 순서를 바꾸며
(예: 저나트륨 목표인데 짠 메뉴를 몇 번 눌러도 "짠 걸 더"로 배우지 않는다), 이유 문장은 사용자
자신의 행동 횟수로만 말한다("최근 저장·클릭 5개 중 4개가 샐러드 메뉴"). 한 가지만 보여주지 않도록
가끔 "평소와 다른 선택지"를 섞고, 그렇다고 밝힌다. 효과 측정을 위해 로그인 사용자의 15%는 학습
없이 규칙만 받는다(사용자 id로 고정 배정).

유료(Standard·High 이용권) 사용자는 Claude(Anthropic API)가 고르고 이유를 쓴다. 이때 위 추천 설정(목표·상한·
성별·나이·키·몸무게·활동량·알레르기·싫어하는 것)과 최근 자주 본 브랜드 이름이 **닉네임·계정 id
없이** 전송된다. 결과는 1시간 캐시되고 계정 삭제 시 함께 지워진다. AI 호출이 실패하거나 이용권의 AI
사용량·하루 상한에 걸리면 무료 방식으로 대체되고 화면에 그렇다고 알린다 — 추천이 비는 일은 없다.

**AI 메모리** (유료) — 추천을 만들 때 같은 AI 호출이 최근 뺀·저장한 메뉴에서 오래 유지될 취향을
한 줄씩 뽑아 기억한다(예: "매운 양념 메뉴는 자주 뺀다"). 추가 호출은 없다. 새로 기억하면 추천 칸에
"기억해 둘게요"로 바로 알리고, "AI가 기억하는 것" 목록에서 언제든 보고 지우거나 직접 한 줄을 적을
수 있다. 건강 상태·질병을 추측하는 내용은 기억하지 않도록 지시하며, 사용자당 20개까지(넘치면 AI가
적은 오래된 것부터 삭제)만 둔다. 요금제를 내려도 남은 기억은 보고 지울 수 있고, 계정 삭제 시 함께
지워진다.

**대화로 찾기** — 로그인 사용자는 "700kcal 이하 치킨", "매운 거 말고"처럼 말로 조건을 걸어 메뉴를
찾는다. 무료는 서버 안의 규칙이 문장을 조건으로 바꿀 뿐이고 외부로 아무것도 보내지 않는다. 유료는
Claude가 대화하며 고르는데, 이때 대화 내용(최근 6턴, 입력 상한에 맞게 앞 턴부터 잘림)과 위 추천 설정·AI
메모리가 계정 식별자 없이 Anthropic API로 전송된다. 대화 내용은 우리 서버에 저장하지 않는다(브라우저를 닫으면
사라진다). 대화에서 드러난 취향은 AI 메모리에 한 줄로 남을 수 있고, 이것도 "AI가 기억하는 것"에서 보고 지울 수
있다. 유료 AI 대화는 요금제별 하루 상한과 이용권의 AI 사용량 안에서 동작하고, 넘으면 무료 방식으로 답하며
"다 써서 조건 검색으로 답했다"고 알린다.

**요금제** (`#plans`, `app/billing/`) — 셋이다. 자동 갱신은 없고 매번 30일 선불 이용권을 산다(연장은 현재
만료일 다음부터).

| | Free | Standard | High |
|---|---|---|---|
| 가격(부가세 포함, 30일) | 0원 | 3,900원 | 9,900원 |
| 맞춤 추천 | 설정·기록 학습형 | AI(Claude Haiku 4.5) | AI(Claude Sonnet 5) |
| 대화로 찾기 | 조건 검색 | AI 대화 | AI 대화 |
| AI 메모리 | 남은 것 보기·지우기만 | 있음 | 있음 |
| 하루 AI 호출 상한 | — | 15회 | 30회 |

**손해 없음이 구조로 보장된다.** 이용권마다 AI 예산(원)이 있다 — 순매출(가격 ÷ 1.1 × 0.96)의 50%. 모든 AI
호출은 부르기 전에 "이 호출이 최대로 들 수 있는 비용"(입력 상한 8,000토큰 + 출력 상한 `max_tokens`, 환율
1달러 1,600원 고정)을 이용권에서 한 SQL 문으로 예약하고, 모자라면 부르지 않는다. 호출 뒤 실제 사용량으로
정산한다. 캐시 적중은 예산을 쓰지 않는다. 그래서 이용권 하나가 낼 수 있는 AI 비용은 선불 금액의 절반을 넘지
못한다. 남은 양은 "이번 기간 AI 남은 양 63%"처럼 글자로만 보여준다(막대·링 없음).

**결제** — 토스페이먼츠 일회성 카드 결제. 금액은 서버 요금제 설정만 쓰고, 프론트가 보낸 금액은 승인 전에 대조만
한다. 같은 주문을 두 번 승인해도 이용권은 하나다. 취소·환불 웹훅이 오면(토스에 다시 조회해 확인) 이용권을 즉시
무효화한다. 이용권 사용 중 다른 요금제 구매(업그레이드)는 아직 막는다. 결제 기록은 회계용이라 탈퇴해도 남기되
계정 식별자만 지운다(익명화).

**환불 정책(미확정)** — AI를 쓰기 시작한 뒤의 청약철회를 어떻게 할지 정하지 않았다. 디지털 콘텐츠의 청약철회
제한은 사전 고지가 필요하므로 라이브 결제 전에 전문가 확인이 필요하다. 실결제(라이브 키)는 사업자등록·통신판매업
신고·토스 계약 뒤다. 그 전까지 테스트 키로만 돈다.

**외부 전송 정리** — 무료: 없음. 유료: 추천 설정·최근 본 브랜드·AI 메모리·대화 내용(계정 식별자 없이)이
Anthropic API로. 결제: 주문번호·금액·(토스가 발급한) 결제 키가 토스페이먼츠로. 카드 정보는 우리 서버를 거치지
않는다(토스 결제창).

## Brand Commitments

- 이름 **Dining Maps**, 워드마크와 로고 마크(`src/components/Logo.jsx`) 사용 중.
- **한국어 전용.** i18n을 전제하지 않는다.
- **지도는 카카오맵 JS SDK 고정.** 다른 지도 제공자로의 교체를 전제하지 않는다(새 도메인은 카카오 개발자 콘솔에 등록해야 지도가 뜬다).
- 현재 문구 톤: 사실 위주, 과장 없음("내 주변 프랜차이즈, 목표에 맞는 메뉴 찾기"). 명시적으로 승인된 보이스 가이드는 아직 없다.

## Evidence on Hand

**실제 데이터** (README 기준 — 수치는 배치마다 갱신된다)

| 항목 | 수치 |
|---|---|
| 브랜드 | 26 (버거·치킨·커피·샐러드·피자·아이스크림 등) |
| 메뉴 | 4,469 |
| 영양정보 | 23,265 |
| 매장 위치 | 18,351 (전국) |
| 브랜드 조사 대장 | 46곳 (확보 완료 26곳) |

**문서 근거** — `docs/diet_score.md`(등급 근거·수식·v1→v3 변천), `docs/data_quality.md`(검증 룰·파서버그 휴리스틱), `docs/brand_survey.md`(브랜드 조사 방법론), `docs/price_data_options.md`(가격 확보 실패 기록), `data/brand_survey.csv`(재실행 가능한 조사 대장), `docs/논문`(등급 기준 인용 원문).

**검증된 사례** — 단백질↔나트륨 컬럼을 일부러 뒤바꾼 테스트에서 188건 전부 `suspected_parser_bug`로 분류돼 적재가 차단됐다. 실제 사고 2건(롯데리아 컬럼 밀림, 아메리카노 A등급)이 이 게이트를 만든 계기다.

**없는 것 — 지어내지 말 것**

- 사용자 후기·평점·리뷰·테스티모니얼 없음. 사용자 수·트래픽 지표 없음.
- 가격, 세트 메뉴 영양값, 맘스터치 버거 외 카테고리 없음.
- 등급 임계값의 실운영 검증 없음(경험적 추정치). 인용 논문 중 이홍기 외(2004)는 표본 26명이며 논문 스스로 한계를 밝힌다.
- 장기 추이(예: "1년간 나트륨 변화") 없음 — 스냅샷을 이제 막 쌓기 시작했다.
- 제휴·브랜드 공식 협력 관계 없음. 브랜드 로고는 공개 자료 인용이며 승인받은 사용이 아니다.

`docs/PROJECT_CONTEXT.md`의 수치·스택 일부(SQLite, 브랜드 5곳)는 갱신 전 상태다. 충돌 시 README와 실제 코드가 우선이다.

## Product Principles

1. **판단은 근거와 함께 보인다.** 등급 문자만 던지지 않는다. 무엇을 기준으로, 어느 시점 데이터로 나온 값인지가 같은 화면 안에 있다.
2. **모르는 것은 모른다고 말한다.** 가격 없음, 세트 제외, 수기 전사 — 결손을 빈칸이나 그럴듯한 추정으로 메우지 않는다.
3. **현장의 1분이 기준이다.** 이동 중 모바일에서 메뉴 판단까지 도달하는 시간이 다른 어떤 정교함보다 우선한다.
4. **절대와 상대는 함께 산다.** 근거의 절대 기준과 UX를 위한 상대 밴드 중 하나만 남기는 단순화는 이 제품의 핵심 논거를 깎는다.
5. **데이터의 신선도는 제품의 일부다.** 배치 주기와 기준일은 감추는 사정이 아니라 표시하는 사실이다.

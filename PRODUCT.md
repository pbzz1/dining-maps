# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

**1순위 — 외식하러 나온 실사용자.** 이동 중 모바일에서, 지금 있는 자리 주변의 프랜차이즈 매장과 그 메뉴가 내 목표(다이어트·단백질 등)에 맞는지 빠르게 판단하려는 사람. 한 손 조작·짧은 체류가 기본 조건이다.

**2순위 — 채용 담당자와 멘토.** 데스크톱에서 이 서비스를 열어 데이터 파이프라인의 깊이와 판단 근거를 확인한다. 실사용자 경험을 희생해 근거를 전시하지는 않되, 데스크톱 화면도 "제대로 만든 것"으로 성립해야 한다.

두 청중의 우선순위는 확정: **실사용자가 먼저이고, 그 완성도 자체가 포트폴리오 증거가 된다.**

## Product Purpose

브랜드마다 흩어지고 형식도 제각각인 프랜차이즈 메뉴 영양정보를 **동일한 기준으로 정규화해 비교 가능하게 만들고**, 그 결과를 **지도 위 내 주변 매장**과 연결한다.

성공 = 사용자가 지금 주변에서 목표에 맞는 메뉴를 근거와 함께 골라내는 것. 부수적으로, 그 판단을 떠받치는 수집→검증→적재→가공 파이프라인이 서비스 화면에서 드러나는 것.

기획 이력상 최초 가설("다이어트 중 갈 식당이 없다")은 검증되지 않은 개인적 추정이라는 지적으로 폐기됐고, 현재 문제 정의는 46개 브랜드를 실제 조사하며 데이터로 확인한 사실에 근거한다. 이 재정의를 되돌리지 않는다.

## Positioning

**브랜드 공식 영양정보를 100kcal 기준으로 정규화하고, 그 위에 절대·상대 이중 등급을 얹은 뒤, 매장 위치와 붙여 놓은 곳.** 이웃 서비스가 그대로 베낄 수 없는 지점은 셋이다.

- **100g이 아니라 100kcal 기준 정규화** — 샐러디가 중량(g)을 공개하지 않아 100g 기준으로는 공정 비교가 깨진다. 모든 브랜드가 가진 칼로리를 축으로 삼았다.
- **점수는 절대(WHO·식약처·논문 인용 고정값), 등급 밴드만 상대화** — 절대만 쓰면 패스트푸드 특성상 거의 전부 D, 상대만 쓰면 근거가 약하다. 둘 다 저장한다.
- **적재 전 품질 게이트** — 파서가 조용히 깨져도 UPSERT가 정상 데이터를 덮어쓰지 않는다. "한 브랜드 항목의 30% 초과가 동시에 같은 필드에서 움직이면 파서 버그"라는 판정 휴리스틱을 쓴다.

## Operating Context

- **모바일 우선, 데스크톱도 제대로.** 실사용은 이동 중 모바일, 심사는 데스크톱. 양쪽 모두 완성도가 필요하다.
- 진입 지점은 지도다(`#map`이 기본 뷰). 위치 기반 반경 검색 → 매장 → 메뉴·등급으로 내려가는 흐름.
- 현재 화면 구성: 지도 / 맞춤 추천 / 신메뉴 / 대시보드 / 매장 목록 5개 뷰, URL 해시가 곧 현재 뷰(공유 가능).
- 데이터는 실시간이 아니다. 월 1회(메뉴)·주 1회(매장) 배치로 갱신되며, 화면에는 마지막으로 품질 게이트를 통과한 크롤 날짜가 기준일로 표시된다.
- 배포는 CloudFront(S3) → Lambda Function URL(FastAPI) → Neon PostgreSQL. 리전 제약(ap-southeast-2)과 DB 왕복 ~90ms 때문에 화면당 API 호출 수가 곧 체감 속도다. Function URL 응답 상한 6MB — 전국 매장을 한 번에 부르는 화면은 성립하지 않고, 프론트는 항상 반경을 넘긴다.

## Capabilities and Constraints

**있는 것**

- 브랜드 목록 / 브랜드별 메뉴 + 영양정보 + 등급 / 브랜드 평균·등급 요약
- 좌표·반경 기반 주변 매장 조회, 등급 필터(절대·상대 선택), 거리순 정렬
- 메뉴 탐색기, 목표별 메뉴 추천(신체정보 기반 BMR 포함), 신메뉴 피드, 품질·통계 대시보드
- 등급 이중 체계: `score`(절대) + `absolute_grade` + `relative_grade` + `percentile`을 함께 저장·표시
- 영양소는 고정 컬럼이 아닌 key-value 행(`nutrition_fact`)으로 저장 — 브랜드마다 공개 항목이 다르기 때문(대부분 5종, 샐러디 7종, 포케올데이 9종)
- 이력·품질 테이블(append-only): 크롤 실행, 메뉴/영양 스냅샷, 품질 체크, 변경 로그

**용어** — 등급 A~D, 절대 등급 / 상대 등급, 다이어트 점수, 기준일, 브랜드(=프랜차이즈), 매장(=지점).

**제약**

- 100kcal 미만 메뉴는 등급 산정에서 제외(블랙커피가 밀도 왜곡으로 A가 되는 문제).
- 세트 메뉴는 전면 제외 — 칼로리 "범위"만 공개돼 고정 영양값이 없다.
- 가격 데이터는 없다. 46개 브랜드 중 영양정보와 가격을 함께 공개하는 곳은 2곳뿐이라 슈링크플레이션 기획을 접었다.
- 맘스터치는 영양정보를 이미지로만 공개해 수기 관리(버거류만 반영), 버거킹은 WAF 차단으로 수동.
- `menu_item`은 UPSERT만 하고 DELETE하지 않아 단종 메뉴가 남아 있을 수 있다.
- 매장 좌표는 카카오맵 기준이라 폐업 미삭제·중복 등록이 섞일 수 있다.
- 절대 기준 A등급은 사실상 존재하지 않는다("프랜차이즈에 흠잡을 데 없는 다이어트 메뉴는 없다"는 결과 자체이기도 하다). 나트륨 기준은 다른 지표보다 가혹하게 작동한다.
- 사용자 행동 로그는 GA4 이벤트 수준만 있고, KPI(참여율·필터 사용률·전환율)를 산출할 집계는 아직 없다.

**미확정** — 접근성 기준(WCAG 등급 등)은 정해진 바 없음. 인증·로그인 없음(개인화는 로컬 저장 기반).

## Brand Commitments

- 이름 **Dining Maps**, 워드마크와 로고 마크(`src/components/Logo.jsx`) 사용 중.
- **한국어 전용.** i18n을 전제하지 않는다.
- **지도는 카카오맵 JS SDK 고정.** 다른 지도 제공자로의 교체를 전제하지 않는다(새 도메인은 카카오 개발자 콘솔에 등록해야 지도가 뜬다).
- 현재 문구 톤: 사실 위주, 과장 없음("내 주변 프랜차이즈, 목표에 맞는 메뉴 찾기"). 명시적으로 승인된 보이스 가이드는 아직 없다.

## Evidence on Hand

**실제 데이터** (README 기준 — 수치는 배치마다 갱신된다)

| 항목 | 수치 |
|---|---|
| 브랜드 | 16 (버거·치킨·커피·샐러드·피자·아이스크림) |
| 메뉴 | 2,220 |
| 영양정보 | 12,152 |
| 매장 위치 | 18,321 (전국) |
| 브랜드 조사 대장 | 46곳 (확보 완료 16곳) |

**문서 근거** — `docs/diet_score.md`(등급 근거·수식·v1→v3 변천), `docs/data_quality.md`(검증 룰·파서버그 휴리스틱), `docs/brand_survey.md`(브랜드 조사 방법론), `docs/price_data_options.md`(가격 확보 실패 기록), `data/brand_survey.csv`(재실행 가능한 조사 대장), `docs/논문`(등급 기준 인용 원문).

**검증된 사례** — 단백질↔나트륨 컬럼을 일부러 뒤바꾼 테스트에서 188건 전부 `suspected_parser_bug`로 분류돼 적재가 차단됐다. 실제 사고 2건(롯데리아 컬럼 밀림, 아메리카노 A등급)이 이 게이트를 만든 계기다.

**없는 것 — 지어내지 말 것**

- 사용자 후기·평점·리뷰·테스티모니얼 없음. 사용자 수·트래픽 지표 없음.
- 가격, 세트 메뉴 영양값, 맘스터치 버거 외 카테고리 없음.
- 등급 임계값의 실운영 검증 없음(경험적 추정치). 인용 논문 중 이홍기 외(2004)는 표본 26명이며 논문 스스로 한계를 밝힌다.
- 장기 추이(예: "1년간 나트륨 변화") 없음 — 스냅샷을 이제 막 쌓기 시작했다.
- 제휴·브랜드 공식 협력 관계 없음. 브랜드 로고는 공개 자료 인용이며 승인받은 사용이 아니다.

`docs/PROJECT_CONTEXT.md`의 수치·스택 일부(SQLite, 브랜드 5곳)는 갱신 전 상태다. 충돌 시 README와 실제 코드가 우선이다.

## Product Principles

1. **판단은 근거와 함께 보인다.** 등급 문자만 던지지 않는다. 무엇을 기준으로, 어느 시점 데이터로 나온 값인지가 같은 화면 안에 있다.
2. **모르는 것은 모른다고 말한다.** 가격 없음, 세트 제외, 수기 전사 — 결손을 빈칸이나 그럴듯한 추정으로 메우지 않는다.
3. **현장의 1분이 기준이다.** 이동 중 모바일에서 주변 매장→메뉴 판단까지 도달하는 시간이 다른 어떤 정교함보다 우선한다.
4. **절대와 상대는 함께 산다.** 근거의 절대 기준과 UX를 위한 상대 밴드 중 하나만 남기는 단순화는 이 제품의 핵심 논거를 깎는다.
5. **데이터의 신선도는 제품의 일부다.** 배치 주기와 기준일은 감추는 사정이 아니라 표시하는 사실이다.
